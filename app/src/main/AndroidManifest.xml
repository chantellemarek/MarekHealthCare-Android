<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cvtc.cmarek5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarekHealthCare">
        <activity android:name=".DatabaseContent"></activity>
        <activity
            android:name=".DatabaseActivity"
            android:label="@string/title_activity_database"
            android:theme="@style/Theme.MarekHealthCare.NoActionBar" />
        <activity android:name=".RecievedActivity" />
        <activity android:name=".DepartmentDescriptionActivity" />
        <activity android:name=".StaffDescriptionActivity" />
        <activity
            android:name=".DepartmentActivity"
            android:label="Our Departments" />
        <activity
            android:name=".ContactActivity"
            android:label="Contact Us" />
        <activity
            android:name=".StaffActivity"
            android:label="Our Staff" />
        <activity
            android:name=".AppointmentActivity"
            android:label="Make an Appointment" />
        <activity
            android:name=".MainActivity"
            android:label="Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>